#pragma once

namespace soup
{
	// math
	struct box_corners;
	struct poly;
	struct ray;

	// mem
	struct alloc_raii_remote;
	struct pattern;
	struct pattern_compile_time_with_opt_bytes_base;
	class pointer;
	class range;
	struct region_virtual;

	// os
	class module;

	// os.windows
	struct handle_raii;
}
